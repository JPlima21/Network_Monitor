services:
  network-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: network-monitor
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
